@model IEnumerable<SmartBus.Models.Conductor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if (User.IsInRole("Admin"))
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
}
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Licencia)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Licencia)
                </td>
                <td>
                    @Html.ActionLink("Details", "Details", new { id = item.ConductorId })

                    @if (User.IsInRole("Admin"))
                    {
                        @Html.Raw(" | ")
                        @Html.ActionLink("Edit", "Edit", new { id = item.ConductorId })
                        @Html.Raw(" | ")
                        @Html.ActionLink("Delete", "Delete", new { id = item.ConductorId })
                    }
                </td>
            </tr>
        }

    </table>
